{
  "name": "",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "mocha test/basic",
    "clean": "rm -rf node_modules && rm -f package-lock.json && npm install"
  },
  "mocha": {
    "timeout": "1800000"
  },

  "devDependencies": {
    "assert": "^2.0.0",
    "chai": "^4.1.2",
    "mocha": "^8.0.1",
    "webdriverio": "^7.2.0"
  }
}
